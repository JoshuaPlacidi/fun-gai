

def encode(model_weights, image_path):
    # TODO takes a image_path and returns the laten representation

    return

def decode(model_weights, latents):
    # TODO takes a latent representation and returns an image

    return